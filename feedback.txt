A few remaining things worth fixing before submission:
The streaming mic path arrow is the weakest element visually. It points diagonally into the middle of the common preprocessing box, which looks like an afterthought. It would read better if it pointed cleanly to the top of the "Raw Audio Input" box with a label like replaces file input — that makes the relationship immediately clear.
The float32 PCM and speech windows connector labels are quite small and in italic grey — on a compressed PDF or printed page these will likely disappear. Either make them slightly larger, or fold that information into the box subtitles instead.
The Result boxes at the bottom have slightly inconsistent framing. The local path says "wake accuracy failed on this clip" which sounds like a bug, but it's actually an SNR/hardware constraint you diagnosed correctly. Rewording to something like "latency architecture validated; wake model needs retraining for low-SNR audio" frames your diagnosis more confidently — which is accurate given your methodology.
Otherwise this is submission-ready. The two-path structure, the shared preprocessing grouping, the measured latency numbers, and the Final Decision box together tell exactly the story the evaluators are looking for.